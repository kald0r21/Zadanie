Na podstawie materiału wideo przedstawiającego ruch drogowy oraz danych z oznaczonymi obiektami (samochodami) w formacie JSON:

Zbuduj system śledzenia obiektów, który:

- wyświetla bounding boxy na pojazdach w nagraniu,

- przypisuje unikalne ID do każdego pojazdu,

- wykrywa i zlicza pojazdy, które przekroczyły zadaną linię (np. wirtualny próg na drodze),

- eksportuje wyniki do pliku .csv (frame, id, class, bbox, crossed_line),

- generuje wynikowy film .mp4 z naniesionymi informacjami.


Instalacja bibliotek :

pip install -r requirements.txt